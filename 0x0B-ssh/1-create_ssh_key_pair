#!/bin/bash

# Generate RSA key pair with specified parameters
ssh-keygen -t rsa -b 4096 -C "Generated RSA key pair" -f school -N "betty"

# Output success message
echo "RSA key pair generated successfully."
